<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.verumomnis.forensic">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- Optional Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VerumOmnis"
        android:usesCleartextTraffic="false">

        <!-- MainActivity -->
        <activity
            android:name="org.verumomnis.forensic.ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Scanner Activity -->
        <activity
            android:name="org.verumomnis.forensic.ui.ScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Verification Activity -->
        <activity
            android:name="org.verumomnis.forensic.ui.VerificationActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Report Viewer Activity -->
        <activity
            android:name="org.verumomnis.forensic.ui.ReportViewerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Legal Advisory Activity -->
        <activity
            android:name="org.verumomnis.forensic.ui.LegalAdvisoryActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="@string/legal_advisory_title" />

        <!-- Security Configuration -->
        <meta-data
            android:name="android.security_config"
            android:resource="@xml/network_security_config" />

    </application>

</manifest>